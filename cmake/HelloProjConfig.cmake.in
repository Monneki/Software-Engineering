@PACKAGE_INIT@

#to change if the library have some dependencies
#includee(CMakeFindDependencyMacro)
#find_dependency(deps)

#Load information for each target
include(${CMAKE_CURRENT_LIST_DIR}/basic_target.cmake)